{% extends 'base.html.twig' %}

{% block title %}New Quiz{% endblock %}

{% block body %}
    <h1>New Quiz</h1>

    {{ form_start(quizForm) }}
        {{ form_row(quizForm.title) }}

        <button type="submit" class="btn" name='Create'>Submit Quiz</button>
    {{ form_end(quizForm) }}
{% endblock %}
